/*import { useDispatch } from 'react-redux'



function ToDoItem({ goal }) {
  const dispatch = useDispatch()

  return (
    <div className='todo'>
      <div>{new Date(todo.createdAt).toLocaleString('en-US')}</div>
      <h2>{todo.text}</h2>
    </div>
  )
}

export default ToDoItem*/
